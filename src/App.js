import styled from "styled-components";

const AppEl = styled.div`
  display: flex;
  min-height: 100vh;
  flex-direction: column;
`;

function App() {
  return <AppEl></AppEl>;
}

export default App;
